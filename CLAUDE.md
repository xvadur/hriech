# hriech — publikácia, mediálna kritika

@AGENTS.md
@PRODUCT.md
@STATUS.md
@STACK.md

Príkazy: dev `pnpm dev` · check `pnpm check` · test `pnpm test` · build `pnpm build` · qa `pnpm qa` · deploy `pnpm run deploy` (externá mutácia).
Kde čo je: `src/` Astro web · `research/` a `private/research/` case files, sourcebooky, kompilácie · `private/drafts/` články (01–04) · `contracts/` schémy · `docs/` · DESIGN.md (redizajn) · `scripts/merge-newsroom-fragments.mjs` mapa redakcií.
Front: `work/`. `private/` je citlivé — publikačné brány platia.
